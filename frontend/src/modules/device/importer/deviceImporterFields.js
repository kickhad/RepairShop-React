import model from 'modules/device/deviceModel';

const { fields } = model;

export default [
  fields.printerBrand,
  fields.model,
  fields.serial,
  fields.orignalOrderNo,
  fields.purchaseDate,
];
